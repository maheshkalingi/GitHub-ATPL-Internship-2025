create table departmentMahesh (
 	dept_id varchar(10) primary key, 
	dept_name varchar(50)
);

create table coursesMahesh (
	course_id varchar(10), 
	course_name varchar(50), 
 	dept_id varchar(10) primary key
);

create table studentsMahesh (
 	student_id varchar(10) primary key, 
 	student_name varchar(50), 
 	dob date, 
	age int, 
  	dept_id varchar(10)
);

insert into departmentMahesh values ('D1', "Computer Science");
insert into departmentMahesh values ('D2', "Mathematics");
insert into departmentMahesh values ('D3', "Physics");

select * from departmentMahesh;

insert into coursesMahesh values ('C1', "DBMS", 'D1');
insert into coursesMahesh values ('C2', "Linear Algebra", 'D2');
insert into coursesMahesh values ('C3', "Quantum Mechanics", 'D3');

select * from coursesMahesh;

insert into studentsMahesh values ('S1', "Ravi", '2002-06-10', 22, 'D1');
insert into studentsMahesh values ('S2', "Sneha", '2001-03-15', 23, 'D2');
insert into studentsMahesh values ('S3', "Arjun", '2003-01-20', 21, 'D1');

select * from studentsMahesh;
alter table coursesMahesh add column duration_in_weeks int;
update studentsMahesh set student_name = 'Snaha Reddy' where student_id = 'S2';
delete from coursesMahesh where course_id = 'C3';