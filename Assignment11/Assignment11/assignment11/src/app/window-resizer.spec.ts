import { WindowResizer } from './window-resizer';

describe('WindowResizer', () => {
  it('should create an instance', () => {
    const directive = new WindowResizer();
    expect(directive).toBeTruthy();
  });
});
